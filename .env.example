# Copy this file to .env and fill in your real keys
RAPIDAPI_KEY=your_rapidapi_key_here
GROQ_API_KEY=your_groq_api_key_here

# Optional overrides (defaults shown)
GROQ_MODEL=openai/gpt-oss-120b
CRAWL_MAX_DEPTH=2
CRAWL_MAX_PAGES=10
CRAWL_DELAY_SECONDS=1.0
REQUEST_TIMEOUT_SECONDS=15
MAX_CONCURRENT_LEADS=10

# Playwright fallback for JS-heavy sites (set to true to enable headless Chromium retry)
CRAWL_PLAYWRIGHT_FALLBACK=false
CRAWL_PLAYWRIGHT_THRESHOLD=800

# Cerebras (set LLM_PROVIDER=cerebras to use)
LLM_PROVIDER=groq
CEREBRAS_API_KEY=your_cerebras_api_key_here
CEREBRAS_MODEL=llama-3.3-70b
